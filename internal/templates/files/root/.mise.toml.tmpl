min_version = "2024.9.0"

[tools]
terraform = "{{ .TerraformVersion | stripConstraint }}"
{{- range .MiseTools }}
{{ .Name }} = "{{ .Version }}"
{{- end }}

[hooks.enter]
shell = "bash"
script = 'command -v tfskel >/dev/null 2>&1 && tfskel init --check 2>/dev/null || true'
